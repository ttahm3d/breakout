import { db } from "../../../configs/firebase";
import { PostType } from "../../../types";

const createPostHandler = async (post: PostType) => {
  try {
  } catch (error) {}
};

const uploadPostPhoto = async (file: any) => {};
